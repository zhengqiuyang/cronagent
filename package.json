{
  "name": "cronagent",
  "version": "0.1.0",
  "description": "Vendor-neutral cron scheduler for AI coding agents — any CLI, run archive, HTML reports",
  "type": "module",
  "license": "MIT",
  "bin": {
    "cronagent": "dist/src/cli.js"
  },
  "files": [
    "dist",
    "demo",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node run-tests.mjs",
    "demo": "node demo/fake-agent.js",
    "prepublishOnly": "npm run build && npm test"
  },
  "keywords": [
    "ai-agents",
    "cli",
    "cron",
    "scheduler",
    "automation",
    "claude-code"
  ],
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^7.0.2"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zhengqiuyang/cronagent.git"
  }
}
